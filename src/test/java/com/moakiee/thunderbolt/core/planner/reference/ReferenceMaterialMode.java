package com.moakiee.thunderbolt.core.planner.reference;

/** The three independently reported inventory modes from the reference standard. */
public enum ReferenceMaterialMode {
    MISSING,
    MINIMUM,
    UNBOUNDED
}
